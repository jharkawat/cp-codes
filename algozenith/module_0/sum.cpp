#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define IOS ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);

signed main ()
{
    IOS
    ll t; 
    cin >> t;
    while(t--)
    {
        ll n,m;
        cin >> n >> m;
        cout << (n+m) << "\n";
    }

}